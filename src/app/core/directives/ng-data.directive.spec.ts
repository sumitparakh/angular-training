import { NgDataDirective } from './ng-data.directive';

describe('NgDataDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDataDirective();
    expect(directive).toBeTruthy();
  });
});
